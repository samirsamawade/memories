let mongoose;_b1c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const postSchema = new mongoose.Schema(
  {
    title: {
      type: String,
      required: true,
    },
    description: {
      type: String,
      required: true,
    },
    budget: {
      type: Number,
      required: true,
    },
    
  },
  { timestamps: true }
);



const posts = mongoose.model('Posts', postSchema);


module.exports = {Posts: posts, Comments: comments};
