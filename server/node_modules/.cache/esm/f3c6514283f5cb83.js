let mongoose;_83f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const {ObjectId} = mongoose.Schema;

const postSchema = new mongoose.Schema(
  {
    title: {
      type: String,
      required: true,
    },
    description: {
      type: String,
      required: true,
    },
    budget: {
      type: Number,
      required: true,
    },
    event: {
      type: ObjectId,
      ref: 'event',
    },
  },
  { timestamps: true }
);

const eventSchema = new mongoose.Schema({
  title: { type: String},
  description: { type: String},
});

const posts = mongoose.model('Posts', postSchema);
const event = mongoose.model('Event', eventSchema);

module.exports = {Posts: posts, Event: comments}
